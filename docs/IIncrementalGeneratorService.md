# IIncrementalGeneratorService

The `IIncrementalGeneratorService` provides the core incremental generation pipeline for source generators, enabling efficient and isolated processing of entity changes in a build pipeline. It coordinates the construction of a generation context, filters relevant entities, and commits generated outputs with minimal overhead.

## API

### Constructor
